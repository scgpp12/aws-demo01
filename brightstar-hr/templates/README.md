# 空白样式 / 空白テンプレート

在本目录放置两个固定 Excel 模板，文件名必须严格如下（部署时会自动同步到
`s3://<bucket>/templates/`，员工发「模板/テンプレ」即可拿到预签名下载链接）：

- `kintai.xlsx` —— 勤怠（考勤）
- `commute.xlsx` —— 通勤費（通勤费用）

> 放好后再 `cdk deploy`；或之后直接覆盖 S3 上同名对象亦可。
